---
const {imageSrc, color} = Astro.props;
---

<div class=`px-12 py-6 flex flex-col items-center bg-gradient-to-t from-[${color}] to-[${color}]`>
  <img class="w-[100px]" src={imageSrc} alt="Category">
  <h3 class="text-[18px] font-semibold">
    <slot />
  </h3>
</div>